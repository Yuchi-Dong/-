# ── 核心框架 ──────────────────────────────────────────
flask>=3.0
waitress>=3.0

# ── 数据库 ────────────────────────────────────────────
psycopg2-binary>=2.9.9

# ── 认证与安全 ────────────────────────────────────────
PyJWT>=2.8.0          # JWT 身份认证
bcrypt>=4.0.0         # 密码哈希（替代 PBKDF2）
cryptography>=42.0    # AES-256-GCM 文件加密 + RSA

# ── 文件处理 ──────────────────────────────────────────
Pillow>=10.0          # 图片处理（头像裁剪/缩略图）
pypdf>=4.0            # PDF 预览/水印
python-docx>=1.1
openpyxl>=3.1
python-pptx>=0.6
mutagen>=1.47         # 音视频元数据

# ── 压缩包支持 ────────────────────────────────────────
rarfile>=4.1
py7zr>=0.21

# ── 速率限制 ──────────────────────────────────────────
flask-limiter>=3.5.0

# ── LDAP/AD 集成 ──────────────────────────────────────
ldap3>=2.9.1

# ── 对象存储（可选）──────────────────────────────────
minio>=7.2.0

# ── 双因素认证 ────────────────────────────────────────
qrcode[pil]>=7.4
pyotp>=2.9.0

# ── 系统信息 ──────────────────────────────────────────
psutil>=5.9

# ── PDF 水印 ──────────────────────────────────────────
reportlab>=4.0
